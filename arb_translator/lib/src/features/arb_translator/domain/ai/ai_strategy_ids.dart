/// Centralized IDs for AI translation strategies.
class AiStrategyIds {
  static const openAi = 'openai';
  static const mock = 'mock'; // simple echo/mock strategy (testing)
  static const offline = 'offline'; // future offline/local model hook
}
